#Create Data Structure

#Input data from postman
{
    "name":string,
    "year":number,
    "author":string,
    "summary":string,
    "publisher",:string,
    "pageCount":number,
    "readPage":number,
    "reading":boolean
}

#Data structure that will be saved in array
{   
    "id":nanoid(16),
    "name":string,
    "year":number,
    "author":string,
    "summary":string,
    "publisher":string,
    "pageCount":number,
    "readPage":number,
    "finished":boolean,
    "reading":boolean,
    "insertedAt":time,
    "updatedAt": time

}

#if finished == true(readPage = pageCount)
#if finished == false(readpage < pageCount)
#if finished == undefined(readpage > pageCount)
